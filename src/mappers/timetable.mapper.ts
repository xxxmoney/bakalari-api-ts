import type {TimeTableSummary} from "../models/timetable-summary.model.js";
import type {TimeTableResponse} from "../models/timetable.model.js";

export function mapTimeTableResponseToSummary(timetable: TimeTableResponse): TimeTableSummary {

}